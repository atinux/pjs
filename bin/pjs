#!/usr/bin/env node

require('commander')
.version(require('../package').version)
.usage('<command> [options]')
.command('start [dir] [options]', 'Launch pjs with the given folder (default ./)', { isDefault: true })
// .command('forever [folder]', 'Launch pjs with forever as a daemon (useful for web-server)')
.parse(process.argv);
